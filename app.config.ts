export default defineAppConfig({
  ui: {
    colors: {
      // 네이비 계열 — 신뢰감 있는 프로페셔널 톤
      primary: 'blue',
      // 누가봉(캐러멜/골든) 계열 — 따뜻한 강조색
      secondary: 'amber',
      neutral: 'slate',
      success: 'green',
      warning: 'amber',
      error: 'red',
      info: 'sky'
    }
  }
})
